{
  "version": 2,
  "installCommand": "cd /vercel/path0 && npm install --include=dev && cd /vercel/path0/shared && npm install --include=dev && cd /vercel/path0/frontend && npm install --include=dev",
  "buildCommand": "cd /vercel/path0/shared && npm run build && cd /vercel/path0/frontend && npm run build && cd /vercel/path0",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
