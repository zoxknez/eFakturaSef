{
  "version": 2,
  "installCommand": "npm install && npm install --prefix shared && npm install --prefix frontend",
  "buildCommand": "npm run build --prefix shared && npm run build --prefix frontend && npm run vite:build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
