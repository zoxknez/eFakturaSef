{
  "version": 2,
  "installCommand": "cd /vercel/path0 && npm install && cd /vercel/path0/shared && npm install && cd /vercel/path0/frontend && npm install",
  "buildCommand": "cd /vercel/path0/shared && npm run build && cd /vercel/path0/frontend && export PATH=$PATH:/vercel/path0/node_modules/.bin && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
